import 'package:flutter/material.dart';

class HMPage extends StatelessWidget {
  const HMPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text('H&M Page')),
    );
  }
}