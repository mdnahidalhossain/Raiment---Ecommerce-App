import 'package:flutter/material.dart';

class LevisPage extends StatelessWidget {
  const LevisPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text('Levis Page')),
    );
  }
}