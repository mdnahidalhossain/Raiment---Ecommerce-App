import 'package:flutter/material.dart';

class ZaraPage extends StatelessWidget {
  const ZaraPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text('Zara Page')),
    );
  }
}